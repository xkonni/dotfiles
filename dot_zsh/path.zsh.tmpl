paths=(/usr/local/go/bin)
paths_pre=($HOME/bin $HOME/.local/bin $HOME/go/bin)

{{- if eq .chezmoi.os "darwin" }}
paths_pre+="/opt/homebrew/opt/make/libexec/gnubin"
{{- end }}

for p in ${paths[@]}
do
    if [ -d $p ]
    then
        if [[ ! $PATH =~ $p ]]
        then
            export PATH=$p:$PATH
        fi
    fi
done

for p in ${paths_pre[@]}
do
    if [ -d $p ]
    then
        PATH="${PATH//$p:/}"
        export PATH=$p:$PATH
    fi
done
